const state = {
  testState: ''
};

const getters = {
  testGetters: () => {
    // testGetters
  }
};

const mutations = {
  testMutation() {
    // testMutation
  }
};

const actions = {
  testActions() {
    // testActions
  }
};

export default {
  namespaced: true,
  state,
  getters,
  mutations,
  actions
}
